module Command.Daicker.Parser (parseArg, pRootCommand, RootCommand (..)) where

import Command.Daicker.Parser.Argument (parseArg)
import Command.Daicker.Parser.Option (RootCommand (..), pRootCommand)
